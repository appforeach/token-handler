version: '3.8'

services:
  keycloak:
    image: quay.io/keycloak/keycloak:latest
    command: start-dev
    environment:
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://postgres:5432/keycloak
      KC_DB_USERNAME: keycloak
      KC_DB_PASSWORD: password
    ports:
      - "8080:8080"
    depends_on:
      - postgres

  postgres:
    image: postgres:latest
    environment:
      POSTGRES_DB: keycloak
      POSTGRES_USER: keycloak
      POSTGRES_PASSWORD: password
    volumes:
      - postgres_data:/var/lib/postgresql/data

  api:
    build:
      context: .
      dockerfile: Poc.Api/Dockerfile
    ports:
      - "5001:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - Keycloak__Authority=http://keycloak:8080/realms/poc
      - Keycloak__Audience=poc-api
    depends_on:
      - keycloak

  yarp:
    build:
      context: .
      dockerfile: Poc.Yarp/Dockerfile
    ports:
      - "5000:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    depends_on:
      - api

  # frontend:
  #   build:
  #     context: ./poc-frontend
  #     dockerfile: Dockerfile
  #   ports:
  #     - "3000:80"
  #   depends_on:
  #     - yarp

  # auth-proxy:
  #   build:
  #     context: ./auth-proxy
  #     dockerfile: Dockerfile
  #   ports:
  #     - "4000:4000"
  #   environment:
  #     - KEYCLOAK_URL=http://keycloak:8080
  #     - KEYCLOAK_REALM=poc
  #     - KEYCLOAK_CLIENT_ID=poc-api
  #     - KEYCLOAK_CLIENT_SECRET=your-client-secret
  #   depends_on:
  #     - keycloak

volumes:
  postgres_data: